{
  "name": "oic-calculator",
  "version": "2.0.0",
  "description": "Calculator that determines if a taxpayer is eligible for an Offer in Compromise with the IRS for tax debts.",
  "main": "gulpfile.js",
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-load-plugins": "^1.1.0",
    "gulp-sass": "^2.1.0"
  },
  "scripts": {
    "start": "gulp",
    "build": "gulp sass"
  },
  "dependencies": {
    "gulp-autoprefixer": "^3.1.0"
  }
}
